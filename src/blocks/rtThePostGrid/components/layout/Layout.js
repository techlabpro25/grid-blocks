import {
    PanelBody, 
} from "@wordpress/components";


function Layout(props) {
    
    return (
        <PanelBody title="Layout Type">
            hello     
        </PanelBody>
    );
}

export default Layout;